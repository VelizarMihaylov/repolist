import { Meta, Story } from '@storybook/react';

import {{pascalCase ComponentName}} from '..';

import { {{pascalCase ComponentName}}Props } from '../{{pascalCase ComponentName}}';

export default {
  component: {{pascalCase ComponentName}},
  title: '{{pascalCase ComponentName}}',
  argTypes: {},
} as Meta;

const Template: Story<{{pascalCase ComponentName}}Props> = (args): React.ReactElement => {
  return <{{pascalCase ComponentName}} {...args} />;
};

export const _default = Template.bind({});

_default.args = {};
