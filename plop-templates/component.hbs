import styled from 'styled-components';

export type {{pascalCase ComponentName}}Props = {
  children?: React.ReactNode;
};

const Styled{{pascalCase ComponentName}} = styled.div``;

const {{pascalCase ComponentName}}: React.FC<{{pascalCase ComponentName}}Props> = ({ children }) => {
  return <Styled{{pascalCase ComponentName}}>{children}</Styled{{pascalCase ComponentName}}>;
};

export default {{pascalCase ComponentName}};
